{# Gate prompt template — rendered by loom as the human gate's
   prompt. Loom passes the upstream task's output via the
   `upstream` context bag.

   The actual report file lives at
   `${upstream.assemble.output.report_path}` and is the rich
   document the user reviews and edits.
#}
# Skill review ready

The automated lint and sub-agent checks have produced a report:

    {{ upstream.assemble.output.report_path }}

Counts:

- Errors:   {{ upstream.assemble.output.errors }}
- Warnings: {{ upstream.assemble.output.warnings }}
- Info:     {{ upstream.assemble.output.infos }}
