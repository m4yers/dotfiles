"""Pipeline-wide validators (not per-task tool tasks). Currently
hosts ``validate`` — a render-time check that every prompt
template across the create/update/review pipelines renders
cleanly against synthetic upstream outputs.
"""
__all__ = []
