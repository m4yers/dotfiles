version: 2
input:
  type: object
  additionalProperties: false
  properties:
    workspace_abs: {type: string}
    build_system_override:
      type: string
      description: Wins over manifest detection when non-empty.
    cache_prefix:
      type: string
      description: |
        Composed cache prefix from overview-ingest (empty when
        cache_mode is bypass). Cached doc lives at
        `<cache_prefix>/build-detect`.
    cache_mode:
      type: string
      enum: [read-write, read-only, write-only, bypass]
  required: [workspace_abs, build_system_override, cache_prefix, cache_mode]
output:
  type: object
  additionalProperties: false
  properties:
    build_system:
      type: string
      description: Resolved family name, or "unknown".
    provenance:
      type: string
      description: |
        "override:cli", the matched manifest marker (e.g.
        "pyproject.toml:[tool.uv]"), or "none".
    installed_skills:
      type: array
      description: |
        Installed skills named <build_system>-* under ~/.kiro/skills;
        downstream agents read these to learn build verbs.
      items:
        type: object
        additionalProperties: false
        properties:
          name: {type: string}
          path: {type: string}
          description: {type: string}
          scripts:
            type: array
            items: {type: string}
        required: [name, path, description, scripts]
    fallback_build_cmd:
      type: string
      description: Static-matrix build command; empty when unknown.
    cache_hit: {type: boolean}
  required:
    - build_system
    - provenance
    - installed_skills
    - fallback_build_cmd
    - cache_hit
