[project]
name = "notepad"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
    "typer>=0.12",
    "loom",
]

[tool.uv.sources]
loom = { path = "../../../loom/scripts/loom", editable = true }

[tool.uv]
package = false
