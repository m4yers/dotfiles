# Final gate

Review the aggregate change.

## Merged review verdict

`{{ input.verdict }}`

## Pool result

Applied tasks: {{ input.applied_count }}
{% if input.failed_task_ids %}
**Skipped after retry exhaustion:** {{ input.failed_task_ids | join(', ') }} —
the aggregate change is INCOMPLETE.
{% endif %}

## Diff (accumulated pool rounds)

```diff
{{ input.diff_bundle }}
```

## Verify-tests output

stdout tail:

```
{{ input.verify_stdout_tail }}
```

stderr tail:

```
{{ input.verify_stderr_tail }}
```

## Decide

Accept the change (run completes as `DONE`) or abandon (exit
with `DONE_WITH_CONCERNS`).

```bash
LOOM=~/.kiro/skills/home/loomv2/scripts/loom.sh
$LOOM output init "{{ input.__loom.workdir }}" --task final-gate
$LOOM output add  "{{ input.__loom.workdir }}" --task final-gate \
    --set decision=accept \
    --set note=''
```

To abandon:

```bash
$LOOM output add "{{ input.__loom.workdir }}" --task final-gate \
    --set decision=abandon \
    --set note='<why>'
```
