#!/bin/bash
# Unconditionally output a retro reminder for injection into conversation
# context. The LLM decides whether to act on it based on session activity.
echo "[RETRO REMINDER] Any learnings so far? If so, create them and print how many retro items are available"
